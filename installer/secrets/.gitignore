# Defense-in-depth ignore so PAT and secrets-URL files cannot be accidentally
# git-add'd before Plan 02-02 installs the root-level rules in .gitignore.
# Plan 02-02 supersedes this with project-wide rules; this file stays as a
# belt-and-braces guard.
*
!.gitignore
!README.md
!.keep
