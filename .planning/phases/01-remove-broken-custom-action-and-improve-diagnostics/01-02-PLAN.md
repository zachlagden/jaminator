---
phase: 01-remove-broken-custom-action-and-improve-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Jaminator.sln
  - installer/build.ps1
autonomous: true
requirements:
  - INSTALL-03
tags:
  - installer
  - build-pipeline
  - solution-cleanup
must_haves:
  truths:
    - "Jaminator.sln lists exactly 2 projects (Jaminator + Bootstrap); the UpdateCheck project reference is gone"
    - "Jaminator.sln contains no orphan GlobalSection(ProjectConfigurationPlatforms) entries for the deleted UpdateCheck GUID"
    - "installer/build.ps1 contains no $caDll variable, no Test-Path on UpdateCheckCA.CA.dll, and no -d UpdateCheckCaDll=... argument"
    - "installer/build.ps1 retains -bindpath, -ext, -arch, -o, -d Version=..., -d SourceDir=... arguments to wix build (unchanged build mechanics)"
    - "dotnet build Jaminator.sln -c Release exits 0 with no project references missing"
  artifacts:
    - path: "Jaminator.sln"
      provides: "Solution graph with only Jaminator and Bootstrap projects"
      contains_not: ["D8F3B4E5", "UpdateCheck", "installer\\\\UpdateCheck"]
      contains: ["Jaminator.csproj", "Bootstrap.csproj"]
    - path: "installer/build.ps1"
      provides: "MSI build script that no longer references UpdateCheckCA"
      contains_not: ["$caDll", "UpdateCheckCaDll", "UpdateCheckCA.CA.dll"]
      contains: ["wix build", "-bindpath", "-d \"Version=$version\"", "-d \"SourceDir=$binDir\""]
  key_links:
    - from: "installer/build.ps1"
      to: "Jaminator.sln"
      via: "dotnet build $repoRoot\\Jaminator.sln -c $Configuration call (line 32)"
      pattern: "dotnet build.*Jaminator\\.sln"
---

## Phase Goal

**As a** Jam Coding technician, **I want to** install Jaminator by double-clicking the MSI on any classroom laptop, **so that** the install completes without the SFXCA CLR-load failure that rolls back v0.7.4's install path.

<objective>
Clean the build pipeline so that `dotnet build Jaminator.sln` and `installer/build.ps1` no longer build, reference, or pass the deleted `UpdateCheck` custom-action DLL. This is the build-orchestration half of INSTALL-03; the WiX source was cleaned in Plan 1, and the project directory will be deleted in Plan 3.

Purpose: Two edits — `Jaminator.sln` (hand-edit to remove the `UpdateCheck` project block + its four orphan ProjectConfigurationPlatforms entries per RESEARCH.md Pitfall P3) and `installer/build.ps1` (remove the `$caDll` variable, the Test-Path build gate, and the `-d UpdateCheckCaDll=...` argument per D-03). After this plan, the solution and build script reference only the two projects that actually ship in v0.7.5 (Jaminator + Bootstrap), and `installer/build.ps1` is consistent with the cleaned `installer.wxs` from Plan 1.

Output: `Jaminator.sln` reduced from 32 lines to 24 lines (2 projects, 8 config mappings) and `installer/build.ps1` reduced by ~4 lines, ready for the second atomic commit (`chore(installer): remove UpdateCheck project from solution and build script` per D-11 commit 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-remove-broken-custom-action-and-improve-diagnostics/01-CONTEXT.md
@.planning/phases/01-remove-broken-custom-action-and-improve-diagnostics/01-RESEARCH.md
@.planning/phases/01-remove-broken-custom-action-and-improve-diagnostics/01-01-SUMMARY.md
@Jaminator.sln
@installer/build.ps1

<interfaces>
<!-- Exact line-by-line content the executor needs.
     Verified against current files at planning time. -->

Jaminator.sln (32 lines, 3 projects) — VERIFIED CONTENT:

```
Line 1:  Microsoft Visual Studio Solution File, Format Version 12.00
Line 2:  # Visual Studio Version 17
Line 3:  Project("{9A19103F-16F7-4668-BE54-9A1E7A4F7556}") = "Jaminator", "src\Jaminator\Jaminator.csproj", "{B9F1A2C3-1234-4321-ABCD-1234567890AB}"
Line 4:  EndProject
Line 5:  Project("{9A19103F-16F7-4668-BE54-9A1E7A4F7556}") = "Bootstrap", "bootstrap\Bootstrap.csproj", "{C7E2A3D4-2345-5432-BCDE-2345678901BC}"
Line 6:  EndProject
Line 7:  Project("{9A19103F-16F7-4668-BE54-9A1E7A4F7556}") = "UpdateCheck", "installer\UpdateCheck\UpdateCheck.csproj", "{D8F3B4E5-3456-6543-CDEF-3456789012CD}"   ← DELETE
Line 8:  EndProject                                                                                                                                              ← DELETE
Line 9:  Global
Line 10: 	GlobalSection(SolutionConfigurationPlatforms) = preSolution
Line 11: 		Debug|Any CPU = Debug|Any CPU
Line 12: 		Release|Any CPU = Release|Any CPU
Line 13: 	EndGlobalSection
Line 14: 	GlobalSection(ProjectConfigurationPlatforms) = postSolution
Line 15: 		{B9F1A2C3-1234-4321-ABCD-1234567890AB}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
Line 16: 		{B9F1A2C3-1234-4321-ABCD-1234567890AB}.Debug|Any CPU.Build.0 = Debug|Any CPU
Line 17: 		{B9F1A2C3-1234-4321-ABCD-1234567890AB}.Release|Any CPU.ActiveCfg = Release|Any CPU
Line 18: 		{B9F1A2C3-1234-4321-ABCD-1234567890AB}.Release|Any CPU.Build.0 = Release|Any CPU
Line 19: 		{C7E2A3D4-2345-5432-BCDE-2345678901BC}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
Line 20: 		{C7E2A3D4-2345-5432-BCDE-2345678901BC}.Debug|Any CPU.Build.0 = Debug|Any CPU
Line 21: 		{C7E2A3D4-2345-5432-BCDE-2345678901BC}.Release|Any CPU.ActiveCfg = Release|Any CPU
Line 22: 		{C7E2A3D4-2345-5432-BCDE-2345678901BC}.Release|Any CPU.Build.0 = Release|Any CPU
Line 23: 		{D8F3B4E5-3456-6543-CDEF-3456789012CD}.Debug|Any CPU.ActiveCfg = Debug|Any CPU   ← DELETE
Line 24: 		{D8F3B4E5-3456-6543-CDEF-3456789012CD}.Debug|Any CPU.Build.0 = Debug|Any CPU      ← DELETE
Line 25: 		{D8F3B4E5-3456-6543-CDEF-3456789012CD}.Release|Any CPU.ActiveCfg = Release|Any CPU ← DELETE
Line 26: 		{D8F3B4E5-3456-6543-CDEF-3456789012CD}.Release|Any CPU.Build.0 = Release|Any CPU   ← DELETE
Line 27: 	EndGlobalSection
Line 28: 	GlobalSection(SolutionProperties) = preSolution
Line 29: 		HideSolutionNode = FALSE
Line 30: 	EndGlobalSection
Line 31: EndGlobal
Line 32: (trailing newline)
```

After hand-edit: 24 lines total (32 minus 6 lines removed: 2 lines for Project/EndProject + 4 ProjectConfigurationPlatforms entries).
DO NOT use `dotnet sln remove` — RESEARCH.md Pitfall P3 (citing dotnet/sdk#8037) confirms it leaves orphan config entries behind. Hand-edit is the documented clean approach for a 32-line file.

installer/build.ps1 (59 lines) — VERIFIED CONTENT for the edit zone (lines 26-51):

```
Line 26: (blank)
Line 27:     # Ensure EXE + UpdateCheck CA are built
Line 28:     $binDir = "$repoRoot\src\Jaminator\bin\$Configuration\net48"
Line 29:     $caDll = "$repoRoot\installer\UpdateCheck\bin\$Configuration\net48\UpdateCheckCA.CA.dll"   ← DELETE
Line 30:     if (-not (Test-Path "$binDir\Jaminator.exe") -or -not (Test-Path $caDll)) {                ← CHANGE (drop $caDll clause)
Line 31:         Write-Host "Building solution..."
Line 32:         dotnet build "$repoRoot\Jaminator.sln" -c $Configuration | Out-Host
Line 33:     }
Line 34:     if (-not (Test-Path $caDll)) { throw "UpdateCheckCA.CA.dll not produced — DTF packaging failed" }   ← DELETE
Line 35: (blank)
Line 36:     # Build MSI
Line 37:     $outDir = "$repoRoot\build"
Line 38:     New-Item -ItemType Directory -Path $outDir -Force | Out-Null
Line 39:     $msi = "$outDir\Jaminator.msi"
Line 40:     if (Test-Path $msi) { Remove-Item $msi -Force }
Line 41: (blank)
Line 42:     & wix build "$repoRoot\installer\installer.wxs" `
Line 43:         -d "Version=$version" `
Line 44:         -d "SourceDir=$binDir" `
Line 45:         -d "UpdateCheckCaDll=$caDll" `                                                          ← DELETE
Line 46:         -bindpath "$repoRoot\installer" `
Line 47:         -ext WixToolset.UI.wixext `
Line 48:         -ext WixToolset.Util.wixext `
Line 49:         -arch x64 `
Line 50:         -o $msi
Line 51:     if ($LASTEXITCODE -ne 0) { throw "wix build failed" }
```

Edit map (also captured in RESEARCH.md Pitfall P4):
- Line 27 comment: change `# Ensure EXE + UpdateCheck CA are built` → `# Ensure EXE is built` (drop CA reference)
- Line 28 `$binDir = ...`: KEEP
- Line 29 `$caDll = ...`: DELETE entire line
- Line 30 `if (-not (Test-Path "$binDir\Jaminator.exe") -or -not (Test-Path $caDll)) {`: CHANGE to `if (-not (Test-Path "$binDir\Jaminator.exe")) {`
- Lines 31-33: KEEP
- Line 34 `if (-not (Test-Path $caDll)) { throw ... }`: DELETE entire line
- Lines 35-44: KEEP
- Line 45 `-d "UpdateCheckCaDll=$caDll" `: DELETE entire line (including the trailing backtick line-continuation)
- Lines 46-51: KEEP (the `-d "SourceDir=$binDir" `` ` on line 44 still has its trailing backtick, so after line 45 is removed the continuation flows directly to line 46's `-bindpath` — verify the backtick on line 44 is intact)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-edit Jaminator.sln to remove UpdateCheck project + orphan config entries</name>
  <files>Jaminator.sln</files>
  <read_first>
    - Jaminator.sln (current file, 32 lines — read in full to confirm GUIDs match the edit plan)
    - .planning/phases/01-remove-broken-custom-action-and-improve-diagnostics/01-RESEARCH.md (Pitfall P3 at lines 402-414; Code Example E4 at lines 682-703; the "Don't Hand-Roll" table entry on solution-file editing)
    - .planning/phases/01-remove-broken-custom-action-and-improve-diagnostics/01-CONTEXT.md (D-02)
  </read_first>
  <action>
    Hand-edit `Jaminator.sln` to remove all six lines referencing the `UpdateCheck` project (D-02, RESEARCH.md Pitfall P3 + Code Example E4).

    DO NOT use `dotnet sln Jaminator.sln remove installer/UpdateCheck/UpdateCheck.csproj` — this CLI command is known to leave orphan `GlobalSection(ProjectConfigurationPlatforms)` entries behind (dotnet/sdk#8037). Hand-edit only.

    Use the Edit tool. Make two edits:

    Edit A — Remove the Project block (lines 7-8).
    Locate the text:
    ```
    Project("{9A19103F-16F7-4668-BE54-9A1E7A4F7556}") = "UpdateCheck", "installer\UpdateCheck\UpdateCheck.csproj", "{D8F3B4E5-3456-6543-CDEF-3456789012CD}"
    EndProject
    ```
    Replace with empty string. (Both lines and their newline.)

    Edit B — Remove the four orphan ProjectConfigurationPlatforms entries (lines 23-26).
    Locate the contiguous block:
    ```
    		{D8F3B4E5-3456-6543-CDEF-3456789012CD}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
    		{D8F3B4E5-3456-6543-CDEF-3456789012CD}.Debug|Any CPU.Build.0 = Debug|Any CPU
    		{D8F3B4E5-3456-6543-CDEF-3456789012CD}.Release|Any CPU.ActiveCfg = Release|Any CPU
    		{D8F3B4E5-3456-6543-CDEF-3456789012CD}.Release|Any CPU.Build.0 = Release|Any CPU
    ```
    Replace with empty string. Note the leading tab indentation (`\t\t`) on each line — preserve the tab character on the surrounding lines (15-22, 27).

    DO NOT touch:
    - The Jaminator project line (line 3) and its EndProject (line 4)
    - The Bootstrap project line (line 5) and its EndProject (line 6)
    - The Jaminator config entries (lines 15-18, GUID `{B9F1A2C3-1234-4321-ABCD-1234567890AB}`)
    - The Bootstrap config entries (lines 19-22, GUID `{C7E2A3D4-2345-5432-BCDE-2345678901BC}`)
    - SolutionConfigurationPlatforms (lines 10-13), SolutionProperties (lines 28-30), EndGlobal (line 31)

    The structural assertions below (the grep criteria 1-9) are the authoritative source of truth for "did the edit succeed." Do **not** use a raw line count as a verification gate — RESEARCH.md E4 reports "24 lines" while a literal `32 - 6` calculation gives 26; the discrepancy is just blank-line counting, not a real signal. If the grep assertions pass, the edit is correct regardless of line count.

    Threat model note: Solution-file edit has no security surface; it's a build-time MSBuild graph.

    Commit message (D-11 commit 2, shared with build.ps1 edit in Task 2): `chore(installer): remove UpdateCheck project from solution and build script`. Make a SINGLE commit combining the .sln edit and the build.ps1 edit (per D-11 — they belong to the same commit boundary).
  </action>
  <acceptance_criteria>
    1. `grep -c 'D8F3B4E5' Jaminator.sln` returns `0` (was: 5 — line 7 plus 4 config entries).
    2. `grep -c 'UpdateCheck' Jaminator.sln` returns `0` (was: 1 — line 7 only).
    3. `grep -c 'installer.UpdateCheck' Jaminator.sln` returns `0`.
    4. `grep -c '^Project(' Jaminator.sln` returns `2` (down from 3 — Jaminator and Bootstrap remain).
    5. `grep -c 'Jaminator.csproj' Jaminator.sln` returns `1` (preserved).
    6. `grep -c 'Bootstrap.csproj' Jaminator.sln` returns `1` (preserved).
    7. `grep -c 'B9F1A2C3-1234-4321-ABCD-1234567890AB' Jaminator.sln` returns `5` (1 Project line + 4 config entries; preserved).
    8. `grep -c 'C7E2A3D4-2345-5432-BCDE-2345678901BC' Jaminator.sln` returns `5` (preserved).
    9. `grep -c 'EndGlobal$' Jaminator.sln` returns `1` (file structure intact).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/zach/development/personal_clients/jam_coding/jaminator &amp;&amp; \
      test "$(grep -c 'D8F3B4E5' Jaminator.sln)" = "0" \
      &amp;&amp; test "$(grep -c 'UpdateCheck' Jaminator.sln)" = "0" \
      &amp;&amp; test "$(grep -c '^Project(' Jaminator.sln)" = "2" \
      &amp;&amp; test "$(grep -c 'Jaminator.csproj' Jaminator.sln)" = "1" \
      &amp;&amp; test "$(grep -c 'Bootstrap.csproj' Jaminator.sln)" = "1" \
      &amp;&amp; test "$(grep -c 'B9F1A2C3-1234-4321-ABCD-1234567890AB' Jaminator.sln)" = "5" \
      &amp;&amp; test "$(grep -c 'C7E2A3D4-2345-5432-BCDE-2345678901BC' Jaminator.sln)" = "5" \
      &amp;&amp; test "$(grep -c 'EndGlobal$' Jaminator.sln)" = "1" \
      &amp;&amp; echo "Plan 01-02 Task 1 acceptance: PASS"</automated>
  </verify>
  <done>All 9 acceptance criteria pass. The solution file declares only Jaminator + Bootstrap, contains no orphan GUIDs, and is structurally complete.</done>
</task>

<task type="auto">
  <name>Task 2: Remove $caDll references and -d UpdateCheckCaDll arg from installer/build.ps1</name>
  <files>installer/build.ps1</files>
  <read_first>
    - installer/build.ps1 (current file, 59 lines — read in full to confirm line numbers haven't drifted)
    - .planning/phases/01-remove-broken-custom-action-and-improve-diagnostics/01-RESEARCH.md (Pitfall P4 at lines 416-435 — the verified line-by-line edit map)
    - .planning/phases/01-remove-broken-custom-action-and-improve-diagnostics/01-CONTEXT.md (D-03)
  </read_first>
  <action>
    Apply four edits to `installer/build.ps1` per D-03 and RESEARCH.md Pitfall P4. Use the Edit tool. After all edits, the build script must still produce `build/Jaminator.msi` cleanly on Windows.

    Edit A — Update the comment on line 27.
    Locate `    # Ensure EXE + UpdateCheck CA are built` and replace with `    # Ensure EXE is built` (drop the `+ UpdateCheck CA` part).

    Edit B — Delete the $caDll variable assignment.
    Locate `    $caDll = "$repoRoot\installer\UpdateCheck\bin\$Configuration\net48\UpdateCheckCA.CA.dll"` (line 29) and replace with empty string (removing the whole line including its newline).

    Edit C — Simplify the build-gate Test-Path check.
    Locate `    if (-not (Test-Path "$binDir\Jaminator.exe") -or -not (Test-Path $caDll)) {` (line 30) and replace with `    if (-not (Test-Path "$binDir\Jaminator.exe")) {`. (Drop the second clause that referenced the now-deleted `$caDll`.)

    Edit D — Delete the post-build assertion on $caDll.
    Locate `    if (-not (Test-Path $caDll)) { throw "UpdateCheckCA.CA.dll not produced — DTF packaging failed" }` (line 34) and replace with empty string.

    Edit E — Delete the -d UpdateCheckCaDll wix-build argument.
    Locate the line `        -d "UpdateCheckCaDll=$caDll" `` (with the trailing backtick PowerShell line-continuation) at line 45 and replace with empty string. After this edit, the multi-line `& wix build ...` command flows from line 44's `-d "SourceDir=$binDir" `` directly to line 46's `-bindpath "$repoRoot\installer" ``. Confirm the backtick at the end of line 44 is intact after the edit (it provides the line-continuation that was previously chaining to line 45).

    Do NOT touch:
    - The version-parsing logic at lines 18-25
    - The `$binDir` definition at line 28
    - `Write-Host "Building solution..."` + `dotnet build` at lines 31-33
    - The output-directory setup at lines 36-40
    - The `-d "Version=..."`, `-d "SourceDir=..."`, `-bindpath`, `-ext`, `-arch`, `-o` args (lines 43-44, 46-50)
    - The exit-code check at line 51

    Resulting file: ~55 lines (down from 59).

    Threat model note: Build-script edit has no runtime security surface. The script runs only on the build machine.

    Commit (shared with Task 1): SINGLE commit with both Jaminator.sln + installer/build.ps1 changes, message `chore(installer): remove UpdateCheck project from solution and build script` (D-11 commit 2).
  </action>
  <acceptance_criteria>
    1. `grep -c '\$caDll' installer/build.ps1` returns `0` (was: 4 — lines 29, 30, 34, 45).
    2. `grep -c 'UpdateCheckCaDll' installer/build.ps1` returns `0` (was: 1 — line 45).
    3. `grep -c 'UpdateCheckCA' installer/build.ps1` returns `0` (was: 1 — line 29, plus the comment on line 27).
    4. `grep -c 'UpdateCheck' installer/build.ps1` returns `0` (was: 3 — lines 27 comment, 29 path, 34 message).
    5. `grep -c 'wix build' installer/build.ps1` returns `1` (preserved).
    6. `grep -c '\-bindpath' installer/build.ps1` returns `1` (RESEARCH.md Pitfall P4: `-bindpath` MUST stay — it resolves WixUILicenseRtf and EULA.rtf).
    7. `grep -c 'Version=\$version' installer/build.ps1` returns `1` (preserved).
    8. `grep -c 'SourceDir=\$binDir' installer/build.ps1` returns `1` (preserved).
    9. `grep -c 'WixToolset.UI.wixext' installer/build.ps1` returns `1` (preserved — WixUI_Minimal depends on this extension).
    10. `grep -c 'WixToolset.Util.wixext' installer/build.ps1` returns `1` (preserved — LaunchApplication CA depends on Wix4UtilCA_X86 from this extension).
    11. `pwsh -NoProfile -Command "& { . { Get-Content installer/build.ps1 -Raw | Out-Null } }"` exits 0 (syntactic validity — if pwsh unavailable in the executor environment, this check defers to the Windows-side validation in Plan 3; document the deferral in the SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/zach/development/personal_clients/jam_coding/jaminator &amp;&amp; \
      test "$(grep -c '\$caDll' installer/build.ps1)" = "0" \
      &amp;&amp; test "$(grep -c 'UpdateCheckCaDll' installer/build.ps1)" = "0" \
      &amp;&amp; test "$(grep -c 'UpdateCheck' installer/build.ps1)" = "0" \
      &amp;&amp; test "$(grep -c 'wix build' installer/build.ps1)" = "1" \
      &amp;&amp; test "$(grep -c '\-bindpath' installer/build.ps1)" = "1" \
      &amp;&amp; test "$(grep -c 'Version=\$version' installer/build.ps1)" = "1" \
      &amp;&amp; test "$(grep -c 'SourceDir=\$binDir' installer/build.ps1)" = "1" \
      &amp;&amp; test "$(grep -c 'WixToolset.UI.wixext' installer/build.ps1)" = "1" \
      &amp;&amp; test "$(grep -c 'WixToolset.Util.wixext' installer/build.ps1)" = "1" \
      &amp;&amp; { if command -v pwsh &gt;/dev/null 2&gt;&amp;1; then \
             pwsh -NoProfile -Command "Get-Content installer/build.ps1 -Raw | Out-Null" &amp;&amp; echo "PowerShell syntax check: PASS" \
               || { echo "PowerShell syntax check: FAIL"; exit 1; }; \
           else \
             echo "PowerShell syntax check: DEFERRED (pwsh not installed in executor environment; criterion 11 deferred to Plan 01-03 Windows-side checkpoint where installer/build.ps1 is actually invoked)"; \
           fi; } \
      &amp;&amp; echo "Plan 01-02 Task 2 acceptance: PASS"</automated>
  </verify>
  <done>All UpdateCheck references removed from build.ps1; remaining build mechanics (version parsing, dotnet build, wix build with preserved args) intact. PowerShell syntax check passes on the build machine or is deferred to Plan 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev box ↔ MSBuild | `dotnet build Jaminator.sln` walks the solution graph; this plan removes a project from that graph. |
| dev box ↔ WiX CLI | `wix build` reads installer.wxs and resolves `-d` extension variables; this plan drops the now-unused `-d UpdateCheckCaDll=...`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Orphan `{D8F3B4E5-...}` config entries in Jaminator.sln | mitigate | Hand-edit (not `dotnet sln remove`) per RESEARCH.md Pitfall P3 cleanly removes the four `ProjectConfigurationPlatforms` entries alongside the `Project` block. Acceptance criterion 1 grep-asserts zero `D8F3B4E5` references. |
| T-01-06 | Denial of service | Accidentally removing `-bindpath`, `-ext`, or other preserved wix-build args | mitigate | Acceptance criteria 5-10 grep-assert each preserved argument. RESEARCH.md Pitfall P4 explicitly maps which lines stay vs delete. |
| T-01-07 | Tampering | Build script reads from `$repoRoot\installer\UpdateCheck\bin\` (a path that will be deleted in Plan 3) | accept (intentional) | This plan is the prerequisite cleanup — once `$caDll` is removed (Task 2 Edit B/C/D), the build script no longer reads the soon-to-be-deleted directory. Plan 3 then physically deletes it. |
</threat_model>

<verification>
- File-level: Task 1 criteria 1-9, Task 2 criteria 1-11.
- Build-level on Windows (informational; full validation lives in Plan 3):
  - After this plan commit, `dotnet build Jaminator.sln -c Release` MUST exit 0. The Bootstrap and Jaminator projects compile independently; removing UpdateCheck from the solution does not change their dependency graph (none of them import the UpdateCheck assembly).
  - `installer/build.ps1` will still succeed at this commit boundary IF run on Windows (installer.wxs from Plan 1 no longer references UpdateCheckCaDll, and build.ps1 no longer passes it). The `installer/UpdateCheck/` directory still exists on disk at this point — Plan 3 deletes it.
- Artifact-level (lessmsi inspection): NOT in this plan's scope. Plan 3 runs the full pipeline including MSI table inspection.
</verification>

<success_criteria>
- `Jaminator.sln` lists exactly 2 projects with no orphan GUID references (Task 1 criteria 1-9).
- `installer/build.ps1` has no `$caDll`, no `UpdateCheckCaDll`, no `UpdateCheck` references and retains all preserved wix-build arguments (Task 2 criteria 1-10).
- A single commit `chore(installer): remove UpdateCheck project from solution and build script` exists in git history covering both files (D-11 commit 2).
- The repo state at this commit is independently buildable on Windows (`dotnet build Jaminator.sln` succeeds; `installer/build.ps1` succeeds), per D-12.
</success_criteria>

<output>
After completion, create `.planning/phases/01-remove-broken-custom-action-and-improve-diagnostics/01-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Include: files modified (Jaminator.sln + installer/build.ps1), all grep results, line counts before/after, and a one-line note flagging that Plan 3 must run on Windows to invoke `installer/build.ps1` and `lessmsi` for the artifact verification.
</output>
