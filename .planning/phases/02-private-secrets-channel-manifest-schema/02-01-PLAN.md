---
phase: 02-private-secrets-channel-manifest-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Jaminator/Models/Manifest.cs
autonomous: true
requirements:
  - WIFI-01
must_haves:
  truths:
    - "Manifest deserialisation supports an optional top-level `wifi` field"
    - "Each Wi-Fi profile carries SSID, auth mode, hidden flag, auto-connect flag, scope, and a nullable PSK"
    - "Existing manifests with no `wifi` section continue to deserialise cleanly (Manifest.Wifi == null)"
    - "The solution builds (dotnet build) with the new DTOs in place"
    - "D-02: nullable top-level `Manifest.Wifi` of type `WifiEntry?` wraps `Profiles: List<WifiProfileEntry>`, mirroring the existing Cleanup/Wallpaper/Schedule pattern"
    - "D-03: WifiProfileEntry fields + defaults match WIFI-01 verbatim (Ssid, AuthMode=\"WPA2PSK\", Hidden=false, AutoConnect=true, Scope=\"all-users\", Psk nullable string)"
    - "D-15: WifiProfileEntry.ToString() override for PSK masking is intentionally deferred to Phase 3 (no Phase 2 log site interpolates a WifiProfileEntry — documented forward residual)"
    - "D-16: this plan commits as `feat(manifest): add Wifi/WifiProfile DTOs to Manifest model` (step 1 of the 5-commit strategy)"
  artifacts:
    - path: "src/Jaminator/Models/Manifest.cs"
      provides: "WifiEntry + WifiProfileEntry sealed DTO classes and Manifest.Wifi field"
      contains: "public sealed class WifiEntry"
    - path: "src/Jaminator/Models/Manifest.cs"
      provides: "WifiProfileEntry class"
      contains: "public sealed class WifiProfileEntry"
  key_links:
    - from: "src/Jaminator/Models/Manifest.cs::Manifest"
      to: "src/Jaminator/Models/Manifest.cs::WifiEntry"
      via: "[JsonProperty(\"wifi\")] public WifiEntry? Wifi"
      pattern: "JsonProperty\\(\"wifi\"\\)"
    - from: "src/Jaminator/Models/Manifest.cs::WifiEntry"
      to: "src/Jaminator/Models/Manifest.cs::WifiProfileEntry"
      via: "[JsonProperty(\"profiles\")] public List<WifiProfileEntry> Profiles"
      pattern: "List<WifiProfileEntry>"
  preconditions:
    - "Operator pre-conditions are NOT required for this plan (DTO addition only — no runtime fetch involved)"
---

## Phase Goal (MVP user-story form)

Source ROADMAP `**Goal:**` line is currently outcome-shaped, not user-story-shaped ("Jaminator can fetch and deserialise a complete fleet config from two sources... joined in memory at startup..."). The phase delivers the thin vertical slice required by Success Criterion 5: a debug build run on the dev box logs the joined-manifest line with at least one Wi-Fi profile + masked PSK. This plan contributes the DTO foundation that the fetcher (02-03) and the debug log line (02-05) depend on.

<objective>
Extend `src/Jaminator/Models/Manifest.cs` with `WifiEntry` (wrapper holding a `List<WifiProfileEntry>`) and `WifiProfileEntry` (SSID, AuthMode, Hidden, AutoConnect, Scope, Psk), and wire `Manifest.Wifi` as a nullable top-level field. The new DTOs match the WIFI-01 schema verbatim and follow the existing sealed-class + `[JsonProperty("snake_case")]` convention used by every other DTO in the file.

Purpose: D-02 / D-03 lock the DTO shape. The fetcher (plan 02-03) and the debug log line (plan 02-05) compile against these types — without them, nothing downstream lands.

Output: One C# source file modified. No external behaviour change yet — manifests deserialise the same way, with the added affordance that a future `wifi` block in the JSON will populate `Manifest.Wifi`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-private-secrets-channel-manifest-schema/02-CONTEXT.md
@.planning/phases/02-private-secrets-channel-manifest-schema/02-RESEARCH.md
@.planning/phases/02-private-secrets-channel-manifest-schema/02-PATTERNS.md
@src/Jaminator/Models/Manifest.cs
@src/Jaminator/Jaminator.csproj

<interfaces>
<!-- Existing DTO patterns the new WifiEntry/WifiProfileEntry must follow.
     Extracted from src/Jaminator/Models/Manifest.cs — executor uses these as
     the structural template for the new types. -->

From src/Jaminator/Models/Manifest.cs — top-level Manifest class wires nullable wrapper sections:
```csharp
[JsonProperty("wallpaper")] public WallpaperEntry? Wallpaper { get; set; }
[JsonProperty("cleanup")] public CleanupEntry? Cleanup { get; set; }
[JsonProperty("schedule")] public ScheduleEntry? Schedule { get; set; }
```

From src/Jaminator/Models/Manifest.cs — wrapper-with-list pattern (CleanupEntry):
```csharp
public sealed class CleanupEntry
{
    [JsonProperty("tempPaths")] public List<string> TempPaths { get; set; } = new();
    // ... etc
}
```

From src/Jaminator/Models/Manifest.cs — string-typed schema fields with documented enum-like values (CommandEntry):
```csharp
public sealed class CommandEntry
{
    [JsonProperty("shell")] public string Shell { get; set; } = "powershell";
    // ... etc
    [JsonProperty("skipIf")] public string? SkipIf { get; set; }
}
```

Locked decisions to honour:
- D-02: nullable wrapper `Manifest.Wifi`
- D-03: exact field names, types, defaults, and `[JsonProperty]` snake-case bindings
- D-15: PSK masking — `ToString()` override is deferred to Phase 3 per PATTERNS.md, NOT included here
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add WifiEntry + WifiProfileEntry DTOs and wire Manifest.Wifi</name>
  <files>src/Jaminator/Models/Manifest.cs</files>
  <read_first>
    - src/Jaminator/Models/Manifest.cs (current file in full — 121 lines; observe sealed-class + `[JsonProperty]` + nullable-wrapper pattern used by Cleanup/Wallpaper/Schedule/CommandEntry)
    - .planning/phases/02-private-secrets-channel-manifest-schema/02-CONTEXT.md `<decisions>` block — specifically D-02 (wrapper shape) and D-03 (field signatures verbatim)
    - .planning/phases/02-private-secrets-channel-manifest-schema/02-PATTERNS.md lines 22-96 (Manifest.cs analog patterns; canonical literal at lines 80-95)
    - .planning/phases/02-private-secrets-channel-manifest-schema/02-RESEARCH.md `## Code Examples` section "Pattern: WifiEntry + WifiProfileEntry DTOs" (the canonical template; ignore the `ToString()` override per PATTERNS.md line 96 — that's Phase 3)
  </read_first>
  <action>
    Implement per D-02 and D-03 locked decisions:

    1. Add `[JsonProperty("wifi")] public WifiEntry? Wifi { get; set; }` to the top-level `Manifest` class, alongside the existing `Wallpaper`, `Cleanup`, `Schedule` nullable wrapper fields. Place it after the `Schedule` line (logical ordering: it's a new top-level section). Per D-02 the wrapper is nullable so manifests without a `wifi` block continue to deserialise cleanly with `Manifest.Wifi == null`.

    2. Add a new `public sealed class WifiEntry` inside the `Jaminator.Models` namespace with one field: `[JsonProperty("profiles")] public List<WifiProfileEntry> Profiles { get; set; } = new();`. Follow the `CleanupEntry` shape (sealed, single section field, `= new()` default for the list so deserialising a manifest with `"wifi": {}` gives an empty list, not null).

    3. Add a new `public sealed class WifiProfileEntry` with these fields verbatim per D-03 (note `LangVersion = latest` and `Nullable enable` in csproj line 12-13 — `string?` is allowed):
       - `[JsonProperty("ssid")] public string Ssid { get; set; } = "";`
       - `[JsonProperty("authMode")] public string AuthMode { get; set; } = "WPA2PSK";` — string-typed (not enum) per D-03 rationale; documented enum-like values `"WPA2PSK" | "WPA3PSK" | "open"` mirror the `CommandEntry.Shell` precedent at line 88 of the existing file
       - `[JsonProperty("hidden")] public bool Hidden { get; set; }` — bool default false
       - `[JsonProperty("autoConnect")] public bool AutoConnect { get; set; } = true;` — explicit `true` default per D-03
       - `[JsonProperty("scope")] public string Scope { get; set; } = "all-users";` — string-typed, documented enum-like values `"all-users" | "current-user"`
       - `[JsonProperty("psk")] public string? Psk { get; set; }` — nullable; never set from public manifest, populated in memory by the fetcher (per D-03 final bullet)

    4. Add a single XML `/// <summary>` block above the `Psk` field calling out: "Pre-shared key. NEVER set from the public manifest — populated in memory by ManifestFetcher from the private secrets channel." Match the prose style of the existing `CommandEntry.SkipIf` XML doc at lines 91-94 of the existing file.

    5. Do NOT add a `ToString()` override on `WifiProfileEntry`. D-15 commits to the override but PATTERNS.md line 96 explicitly defers it to Phase 3 (when the runner starts string-interpolating entries into log messages). The Phase 2 log emission site (plan 02-05) uses a literal `***` instead of relying on the override.

    6. No new `using` directives needed — `System.Collections.Generic` and `Newtonsoft.Json` are already imported at lines 1-2.

    Constraints — what NOT to do:
    - Do NOT change any existing field on `Manifest` (only ADD `Wifi`).
    - Do NOT make `WifiEntry` an `interface` or `record` — must be `public sealed class` to match the codebase convention.
    - Do NOT add `[Required]` attributes or `JsonConverter`s — Newtonsoft 13.0.3 handles the schema with default settings (verified in RESEARCH.md "Standard Stack" table).
    - Do NOT add `using System.Linq;` — not needed for DTO definitions.
  </action>
  <verify>
    <automated>cd /home/zach/development/personal_clients/jam_coding/jaminator &amp;&amp; grep -c 'public sealed class WifiEntry\|public sealed class WifiProfileEntry\|JsonProperty("wifi")\|JsonProperty("profiles")\|JsonProperty("ssid")\|JsonProperty("authMode")\|JsonProperty("autoConnect")\|JsonProperty("scope")\|JsonProperty("psk")' src/Jaminator/Models/Manifest.cs | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    All of the following must be true. Each is a grep that the executor (or the checker) can run from the repo root on a non-Windows host without compiling.

    1. `grep -c 'public sealed class WifiEntry' src/Jaminator/Models/Manifest.cs` returns `1`.
    2. `grep -c 'public sealed class WifiProfileEntry' src/Jaminator/Models/Manifest.cs` returns `1`.
    3. `grep -c '\[JsonProperty("wifi")\] public WifiEntry? Wifi' src/Jaminator/Models/Manifest.cs` returns `1`.
    4. `grep -c '\[JsonProperty("profiles")\] public List<WifiProfileEntry> Profiles' src/Jaminator/Models/Manifest.cs` returns `1`.
    5. `grep -c '\[JsonProperty("ssid")\] public string Ssid' src/Jaminator/Models/Manifest.cs` returns `1`.
    6. `grep -c '\[JsonProperty("authMode")\] public string AuthMode' src/Jaminator/Models/Manifest.cs` returns `1` AND the line includes `= "WPA2PSK"` (D-03 default).
    7. `grep -c '\[JsonProperty("hidden")\] public bool Hidden' src/Jaminator/Models/Manifest.cs` returns `1`.
    8. `grep -c '\[JsonProperty("autoConnect")\] public bool AutoConnect' src/Jaminator/Models/Manifest.cs` returns `1` AND the line includes `= true` (D-03 explicit default).
    9. `grep -c '\[JsonProperty("scope")\] public string Scope' src/Jaminator/Models/Manifest.cs` returns `1` AND the line includes `= "all-users"`.
    10. `grep -c '\[JsonProperty("psk")\] public string? Psk' src/Jaminator/Models/Manifest.cs` returns `1` (note the `?` for nullable).
    11. `grep -c 'public override string ToString' src/Jaminator/Models/Manifest.cs` returns `0` (the override is DEFERRED to Phase 3 per PATTERNS.md line 96 — must NOT appear in Phase 2).
    12. The file contains zero new `using` directives beyond the existing two (`System.Collections.Generic`, `Newtonsoft.Json`). Verify with `grep -c '^using ' src/Jaminator/Models/Manifest.cs` returning `2`.
    13. On a Windows build box: `pwsh installer/build.ps1 -Configuration Debug` succeeds through `dotnet build` (build will fail until plan 02-02 generates BuildSecrets.g.cs — see plan-02-02 acceptance criteria; an alternative `dotnet build src/Jaminator/Jaminator.csproj` invocation will fail without BuildSecrets.g.cs because no caller references it yet, so this DTO-only commit must build standalone with a stub or without the dependency — checker note: this plan is buildable in isolation because no other code references the new types yet).

    Self-check: at this point, NO C# code references `Manifest.Wifi` yet — the DTOs are passive. Build must succeed standalone on `dotnet build src/Jaminator/Jaminator.csproj` (no PAT/BuildSecrets dependency introduced by this plan).
  </acceptance_criteria>
  <done>
    `src/Jaminator/Models/Manifest.cs` contains the two new sealed classes with all `[JsonProperty]` bindings matching the WIFI-01 schema exactly, the `Manifest.Wifi` field is wired, and the file compiles in isolation (`dotnet build src/Jaminator/Jaminator.csproj` succeeds without the rest of Phase 2 being in place).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary in this plan) | This plan adds passive DTO definitions only. No I/O, no network, no secret handling. All trust boundaries remain those introduced by the fetcher (plan 02-03) and the build script (plan 02-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02 (partial) | Information Disclosure | `WifiProfileEntry.Psk` field exists in process memory once the fetcher (plan 02-03) populates it | mitigate (forward-looking) | The `Psk` field is `string?` and starts `null`. Per D-15 the PSK is masked as `***` in all log paths. Plan 02-05 enforces masked emission at the only Phase 2 logging call site. The full `ToString()` override mitigation (D-15) is intentionally deferred to Phase 3 per PATTERNS.md line 96 — Phase 2 has no consumer that string-interpolates `WifiProfileEntry` into a log message, so the override is not yet load-bearing. This is a documented forward residual: an executor in Phase 3 MUST add the `ToString()` override before the runner logs an entry. |
| T-05 | Logic Error | `WifiProfileEntry.Psk` may be `null` after the fetcher join if the public manifest declares an SSID with no matching secret | mitigate | The DTO makes `Psk` nullable (`string?`), forcing every downstream consumer (the fetcher join in plan 02-03, the runner in Phase 3) to handle the null case explicitly. The Phase 3 runner is responsible for the open-network avoidance — Phase 2's contribution is to NOT default `Psk` to an empty string or any sentinel value that could be mistaken for a valid PSK. |

Other phase-level threats (T-01 PAT-in-source, T-03 cache half-write, T-04 PAT-leak-to-wrong-URL) are out of scope for this plan — they're addressed by plan 02-02 (T-01) and plan 02-03 (T-03, T-04).
</threat_model>

<verification>
After Task 1:
1. The grep gates in `<acceptance_criteria>` all pass.
2. `dotnet build src/Jaminator/Jaminator.csproj` (run on a Windows or Linux host with .NET SDK 8+) succeeds with zero errors and zero warnings related to the new types.
3. The existing `manifest/manifest.json` in the repo (which does not have a `wifi` block) continues to deserialise into a `Manifest` instance with `Manifest.Wifi == null` — verified by smoke-running the Jaminator EXE in UI mode on the dev box at the end of plan 02-05, NOT in this plan.
</verification>

<success_criteria>
- `WifiEntry` and `WifiProfileEntry` sealed classes exist in `src/Jaminator/Models/Manifest.cs` with the exact field signatures from D-03.
- `Manifest.Wifi` is a nullable `WifiEntry?` field bound to `[JsonProperty("wifi")]`.
- File compiles in isolation (no dependency on plans 02-02, 02-03 introduced).
- No `ToString()` override on `WifiProfileEntry` (deferred to Phase 3).
- Maps to Phase 2 ROADMAP Success Criterion 3: "src/Jaminator/Models/Manifest.cs defines a WifiEntry + WifiProfileEntry DTO matching the WIFI-01 schema (SSID, auth mode, hidden, autoConnect, scope, PSK), wired into the top-level Manifest class as wifi" — fully satisfied by this plan.
- Maps to WIFI-01: schema definition (the DTO side; the docs side lands in plan 02-04).
</success_criteria>

<output>
After completion, create `.planning/phases/02-private-secrets-channel-manifest-schema/02-01-SUMMARY.md` per the template referenced in `<execution_context>`.

Commit per D-16 step 1: `feat(manifest): add Wifi/WifiProfile DTOs to Manifest model`. Touch only `src/Jaminator/Models/Manifest.cs`.
</output>
