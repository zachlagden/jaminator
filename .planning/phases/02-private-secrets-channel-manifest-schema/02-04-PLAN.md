---
phase: 02-private-secrets-channel-manifest-schema
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/manifest-schema.md
autonomous: true
requirements:
  - WIFI-01
  - WIFI-03
must_haves:
  truths:
    - "docs/manifest-schema.md documents the new public-manifest `wifi.profiles[]` schema with field types, defaults, and allowed enum-like string values"
    - "docs/manifest-schema.md documents the private `secrets.json` schema (flat SSID→PSK map) and the PAT-bearer fetch mechanism (api.github.com contents endpoint, NOT raw.githubusercontent.com)"
    - "docs/manifest-schema.md documents the cache topology (both files written atomically as a pair under %ProgramData%\\Jaminator\\cache\\)"
    - "docs/manifest-schema.md carries the candid threat-model honesty note from CONTEXT.md and the PROJECT.md WIFI-03 decision"
    - "docs/manifest-schema.md cross-links installer/secrets/README.md (operator runbook) and PROJECT.md (Key Decisions)"
    - "D-01: documents the flat SSID→PSK map shape of `secrets.json` (no envelope, no schemaVersion)"
    - "D-03: documents the WifiProfileEntry field set + allowed AuthMode/Scope string values + nullable PSK"
    - "D-08: documents that the private secrets URL is baked into the binary at build time (not in the public manifest)"
    - "D-10: documents the atomic-pair cache topology under `%ProgramData%\\Jaminator\\cache\\`"
    - "D-15: documents PSK masking (`***`) as the only logged representation, plus the threat-model honesty note (RE-recoverable PAT, `netsh wlan show profile key=clear` exposure)"
    - "D-16: this plan commits as `docs(manifest-schema): document wifi.profiles[] and the private secrets channel` (step 4 of the 5-commit strategy)"
  artifacts:
    - path: "docs/manifest-schema.md"
      provides: "Public wifi.profiles[] schema block + Private secrets.json schema block + Cache topology + Threat-model honesty note"
      contains: "wifi.profiles"
  key_links:
    - from: "docs/manifest-schema.md::Private secrets channel"
      to: "installer/secrets/README.md"
      via: "markdown link"
      pattern: "installer/secrets/README\\.md"
    - from: "docs/manifest-schema.md::Threat model"
      to: ".planning/PROJECT.md Key Decisions (WIFI-03)"
      via: "explicit reference to PROJECT.md"
      pattern: "WIFI-03"
  preconditions:
    - "No code dependencies — this plan is pure documentation and can land in Wave 1 alongside plans 02-01 and 02-02"
---

<objective>
Extend `docs/manifest-schema.md` with four new sections that document the WIFI-01 and WIFI-03 surface introduced in this phase:

1. **Public manifest — `wifi.profiles[]`** — field-by-field schema for the new public-manifest section. Explicit callout: PSKs never appear here.
2. **Private secrets — `secrets.json` (PAT-gated)** — flat SSID→PSK map schema, the GitHub REST API contents endpoint (NOT raw.githubusercontent.com), required PAT scope, URL form, headers, and an operator workflow link to `installer/secrets/README.md`.
3. **Cache topology** — the two-file `%ProgramData%\Jaminator\cache\` pair and the atomic-pair-write semantics with the "between-two-Moves" rare-failure window noted honestly.
4. **Threat model (honesty note)** — the PAT-in-MSI is operational opacity, not cryptographic protection. Mitigation = rotation. Aligned with PROJECT.md WIFI-03 Key Decisions.

Purpose: WIFI-01 explicitly requires "documented in `docs/manifest-schema.md` alongside the existing manifest entries". WIFI-03 implicitly requires docs (operators won't know how to rotate, how to add a new SSID, or what the threat model promises without them). Phase 4 also requires `docs/manifest-schema.md` to be the home of the operator "rotate a PSK" workflow — Phase 2 lays the schema foundation that Phase 4's rotation section will build on.

Output: One markdown file modified. The existing top-level overview, `wallpaper`, `folders`, `programs`, `commands`, `cleanup` sections are preserved unchanged. The new content is appended in the natural reading order (after `cleanup`, before any future schedule/secrets discussion that might land later).

This plan is **independent of the code plans** — it can run in Wave 1 alongside plans 02-01 (DTOs) and 02-02 (build script) because the schema is fully locked by CONTEXT.md D-01 / D-03 / D-08 / D-10. Documenting it before the code exists is fine; the schema is authoritative.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-private-secrets-channel-manifest-schema/02-CONTEXT.md
@.planning/phases/02-private-secrets-channel-manifest-schema/02-RESEARCH.md
@.planning/phases/02-private-secrets-channel-manifest-schema/02-PATTERNS.md
@docs/manifest-schema.md

<interfaces>
<!-- The existing docs/manifest-schema.md style. Extracted from the first 50 lines
     of the file so the executor matches the voice and heading levels. -->

From docs/manifest-schema.md — overall doc voice and structure:
- H1: `# Manifest schema`
- Opening paragraph naming the canonical fetch URL
- H2 sections for `wallpaper`, `folders`, `programs`, `commands`, `cleanup`
- Per-section pattern: short prose intro, JSON example in a fenced ```json block, then prose explaining semantics
- Markdown tables for top-level field summaries (`| Field | Purpose |` style at line 17)
- Casual but authoritative tone ("the manifest at ... is the source of truth")

From .planning/phases/02-private-secrets-channel-manifest-schema/02-PATTERNS.md lines 455-486 — the four sections this plan must add and their exact content boundaries.

Locked decisions that shape the docs content (re-read these before drafting):
- D-01: Private secrets is a flat `{ "SSID": "PSK" }` map. No envelope, no schemaVersion field.
- D-03: WifiProfileEntry fields verbatim (Ssid, AuthMode, Hidden, AutoConnect, Scope, Psk) with snake-case JSON keys and string-typed enum-like values (`"WPA2PSK" | "WPA3PSK" | "open"`, `"all-users" | "current-user"`).
- D-08: Private secrets URL is build-time-baked; documented form is the GitHub REST API contents endpoint.
- D-10: Cache topology — both files under `%ProgramData%\Jaminator\cache\`, atomic-pair write semantics.
- D-15: PSKs masked as `***` in any log path (documenting this prevents future readers from "fixing" it back to plaintext).
- PROJECT.md WIFI-03 Key Decisions row: the candid threat-model statement. Quote it; do not paraphrase.

Canonical material to draw from (do not invent):
- RESEARCH.md Pattern 1 lines 226-270 — the GitHub REST API endpoint shape, headers, PAT scope, rate limits
- RESEARCH.md Pattern 3 lines 314-369 — the atomic-pair cache semantics
- RESEARCH.md Common Pitfalls Pitfall 1 lines 459-468 — the raw.githubusercontent.com anti-pattern (mention by name so future maintainers don't "simplify" the fetch)
- CONTEXT.md `<specifics>` lines 213-215 — joined-log-line wording and the `installer/secrets/README.md` cross-reference
- CONTEXT.md decisions block lines 34-127 — the full set of locked decisions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add four new sections to docs/manifest-schema.md (Wi-Fi profiles + private secrets + cache topology + threat model)</name>
  <files>docs/manifest-schema.md</files>
  <read_first>
    - docs/manifest-schema.md (current 182 lines — observe heading levels, JSON fence style, markdown table format, prose voice)
    - .planning/phases/02-private-secrets-channel-manifest-schema/02-CONTEXT.md `<decisions>` block (D-01, D-03, D-08, D-10, D-15) and `<specifics>` block (joined-log line + operator README cross-reference)
    - .planning/PROJECT.md Key Decisions row "WIFI-03: private manifest repo + PAT-in-MSI (not encryption-at-rest)" (full row text — quote the rationale verbatim in the threat-model section)
    - .planning/REQUIREMENTS.md WIFI-01 and WIFI-03 entries (the requirement-side wording the docs must satisfy)
    - .planning/phases/02-private-secrets-channel-manifest-schema/02-RESEARCH.md `## Architecture Patterns` Pattern 1 (lines 226-270; copy the URL form, headers, PAT scope, rate limits) and Pattern 3 (lines 314-369; copy the atomic-pair write semantics and the documented between-two-Moves window)
    - .planning/phases/02-private-secrets-channel-manifest-schema/02-RESEARCH.md `## Common Pitfalls` Pitfall 1 (lines 459-468 — the raw.githubusercontent.com anti-pattern; reference by name so future maintainers know not to "simplify" the fetch)
    - .planning/phases/02-private-secrets-channel-manifest-schema/02-PATTERNS.md lines 455-486 (content outline for the four new sections — the literal item list)
  </read_first>
  <action>
    Append four new sections to `docs/manifest-schema.md`. Preserve all existing content (lines 1-182) exactly. Match the existing voice, heading levels (H2 for top-level sections, H3 for sub-headings), fenced-code-block language tags (`json`, `csharp`, `http`), and markdown table style.

    Section order (append in this order, each as a new H2):

    ---

    **Section 1: `wifi`** (parallels existing H2 sections like `wallpaper`, `cleanup`)

    Intro paragraph: Wi-Fi profiles deploy via `netsh wlan add profile` (Phase 3 — out of scope for this doc's depth) on every laptop the EXE runs on. The public manifest carries everything EXCEPT the PSK; the PSK is delivered separately via the private secrets channel (link forward to the section below). Authoring a `wifi` block is optional — manifests with no `wifi` field continue to work unchanged.

    JSON example (use the existing wallpaper-section style for fencing):
    ```json
    "wifi": {
      "profiles": [
        {
          "ssid": "SchoolNet-Year3",
          "authMode": "WPA2PSK",
          "hidden": false,
          "autoConnect": true,
          "scope": "all-users"
        }
      ]
    }
    ```

    Field table — match the style of the line-17 top-level fields table:
    ```markdown
    | Field | Type | Default | Notes |
    |-------|------|---------|-------|
    | `ssid` | string | (required) | SSID broadcast by the access point. Case-sensitive — must match exactly what `netsh wlan show networks` reports. |
    | `authMode` | string | `"WPA2PSK"` | Allowed values: `"WPA2PSK"`, `"WPA3PSK"`, `"open"`. String-typed for authoring forgiveness; the Phase 3 runner validates at deploy site. |
    | `hidden` | bool | `false` | Set `true` for non-broadcasting SSIDs (the laptop probes for them explicitly). |
    | `autoConnect` | bool | `true` | Whether Windows should auto-associate to this SSID when in range. |
    | `scope` | string | `"all-users"` | Allowed values: `"all-users"` (fleet-wide profile, the typical case), `"current-user"` (profile visible only to the user who triggered the install). |
    | `psk` | string | (never in public manifest) | Pre-shared key. **Never set this field in the public manifest.** It is populated in memory at runtime from the private secrets channel (see below). |
    ```

    **Important callout** (under the table, before the next section):
    > **PSKs never appear in the public manifest.** The `psk` field is reserved for runtime population from the private secrets channel. If you find yourself writing a PSK into `manifest/manifest.json`, stop — the public manifest lives on `github.com/zachlagden/jaminator` and is indexed by search engines.

    ---

    **Section 2: `Private secrets channel`** (new H2 — parallel to the `wifi` H2 above)

    Intro paragraph: PSKs live in a **separate, private GitHub repo** gated by a fine-grained read-only PAT bundled in the MSI. `ManifestFetcher` fetches the public manifest as today, then fetches the private `secrets.json` using the bundled PAT as a Bearer token, then joins SSID→PSK in memory before returning the `Manifest` to the rest of the application.

    H3 sub-section: **Schema (`secrets.json`)** — flat JSON object, keys are SSIDs (case-sensitive — must match the public manifest's `ssid` field exactly), values are PSKs as strings:
    ```json
    {
      "SchoolNet-Year3": "<the PSK for SchoolNet-Year3>",
      "SchoolNet-Staff":  "<the PSK for SchoolNet-Staff>"
    }
    ```
    No envelope, no `schemaVersion` field, no nested structure. Adding a new SSID is one line.

    H3 sub-section: **Fetch mechanism** — must use the GitHub REST API contents endpoint with Bearer auth and the raw-bytes media type. Document the URL form and headers:
    ```http
    GET https://api.github.com/repos/{owner}/{repo}/contents/{path}?ref={branch}
    Authorization: Bearer <fine-grained-PAT>
    Accept: application/vnd.github.raw+json
    User-Agent: Jaminator/<version>
    X-GitHub-Api-Version: 2026-03-10
    ```
    Anti-pattern callout (call this out explicitly so a future maintainer doesn't "simplify" the fetch back to a raw URL):
    > **Do NOT use `raw.githubusercontent.com`** for the private fetch. It silently ignores `Authorization` headers for private repos — the request will 404 with no indication that auth was even tried, or worse, return a 200 from a public fork that happens to share the name. The REST API contents endpoint is the only host that honours Bearer auth for private content. See the inline code comment in `src/Jaminator/Services/ManifestFetcher.cs` for the canonical fetcher.

    H3 sub-section: **PAT scope** — Generate a **fine-grained** PAT (NOT a classic PAT) with these permissions:
    - **Contents: Read** on the private secrets repo ONLY (not "All repositories")
    - **Metadata: Read** is auto-included by GitHub when any other repo permission is granted
    - No org-level permissions
    - No other repos selected

    H3 sub-section: **Operator workflow** — link forward to `installer/secrets/README.md` for the build-operator setup steps (where to drop the PAT, the env-var fallback, rotation cadence).

    ---

    **Section 3: `Cache topology`** (new H2)

    Intro paragraph: Both the public manifest and the private secrets are cached under `%ProgramData%\Jaminator\cache\` so logon-time runs survive offline classroom periods. The two files are written **atomically as a pair**: both `.tmp` files are written first, then `File.Delete` + `File.Move` is applied per file.

    Layout:
    ```
    %ProgramData%\Jaminator\cache\
    ├── manifest.json   (public — fetched from raw.githubusercontent.com/zachlagden/jaminator/...)
    └── secrets.json    (private — fetched from api.github.com/repos/{org}/{priv-repo}/contents/...)
    ```

    Failure modes:
    - **Both online fetches succeed** → join in memory, atomic-pair-write to cache, return the joined `Manifest`. `fromCache: false`.
    - **Either online fetch fails AND both cached files exist and deserialise** → load from cache, join in memory, return the joined `Manifest`. `fromCache: true`.
    - **Either online fetch fails AND either cached file is missing or corrupt** → throw `InvalidOperationException` naming which file failed and why. The fetch NEVER silently returns a half-populated `Manifest`.

    Documented residual (be candid):
    > A crash between the two `File.Move` calls leaves one cache file fresh and the other stale. On the next successful network fetch the pair re-syncs; on the next offline launch the deserialiser detects the mismatch when the join produces unexpected `null` PSKs for SSIDs the operator expected to be populated. This is rare and benign for v0.8.0; HARDEN-06 (M3 — parallel logon-path I/O) is the planned revisit.

    ---

    **Section 4: `Threat model — operational, not cryptographic`** (new H2 — final section, sets honest expectations)

    Quote (or closely paraphrase) the WIFI-03 Key Decisions rationale from `.planning/PROJECT.md`. The candid claim is:

    > The PAT bundled in the MSI is **operational opacity**, not cryptographic protection. An attacker willing to reverse-engineer the public MSI can recover the PAT and use it to read the private secrets repo. This is **accepted by design**. The threat model:
    >
    > - **In scope (mitigated):** keep PSKs off the public, search-engine-indexable internet. The public `zachlagden/jaminator` repo never contains a PSK. A casual reader of the public repo cannot find PSKs by grepping or searching.
    > - **Out of scope (accepted):** an attacker with the MSI can recover the PAT via static analysis (strings, `dotPeek`, `ILSpy`). The fine-grained PAT is read-only (`Contents: Read`) and scoped to ONLY the private secrets repo, so the blast radius is bounded — the attacker reads PSKs they could already extract from any deployed laptop via `netsh wlan show profile key=clear`. The shared exposure model is what makes the tradeoff acceptable.
    > - **Mitigation:** termly rotation of both PSKs and the PAT, aligned. When a PSK rotates, the PAT rotates too — replace `installer/secrets/wifi-pat.txt`, rebuild the MSI via `installer/build.ps1`, ship a new release.
    >
    > Encryption-at-rest in the public manifest was considered and **rejected**: every variant ends in rot13 because the decryption key would have to be in the publicly-downloadable MSI. WIFI-03 chooses operational security (private repo + PAT) over cryptographic theatre. Full rationale: `.planning/PROJECT.md` Key Decisions row "WIFI-03".

    PSK exposure on the laptop itself: any local admin can run `netsh wlan show profile name=<SSID> key=clear` and recover the PSK in plaintext. This is a Windows-level reality unrelated to Jaminator's design — same exposure as any other Wi-Fi profile distribution method including Intune. The cached `%ProgramData%\Jaminator\cache\secrets.json` file gives a non-admin local user the same data via a different read path; ACLing the cache file to admin-only is not implemented (would add NT-API surface for zero net security gain).

    PSK masking in Jaminator's own logs: PSKs are masked as `***` in every log emission path in Jaminator (`%ProgramData%\Jaminator\logs\jaminator-YYYYMMDD.log` and any `%TEMP%` diagnostic logs added in Phase 4). This is enforced at the emission site, not at the DTO — see plan 02-05 / `src/Jaminator/UI/MainForm.cs` for the canonical masked-log call pattern.

    ---

    Constraints — what NOT to do:
    - Do NOT modify any existing section of `docs/manifest-schema.md` (the existing wallpaper / folders / programs / commands / cleanup sections stay byte-identical).
    - Do NOT include a real PAT or a real PSK as an example value. Use placeholder strings like `"<the PSK for SchoolNet-Year3>"` or `"github_pat_…redacted…"`.
    - Do NOT promise features beyond Phase 2's scope. The `netsh wlan add profile` deployment, idempotency check, and end-to-end smoke test are explicitly Phase 3/4 — do not document them here (a one-liner "deploy via netsh — Phase 3" forward-link is fine, but no schema or behaviour details).
    - Do NOT document HARDEN-07 (CI automation) as if it's shipped — it's M3.
    - Do NOT remove or weaken the threat-model honesty section. The whole point of WIFI-03's design is that it's honest about what it does and doesn't promise. The candid wording protects future maintainers from re-litigating the encryption-at-rest decision.
  </action>
  <verify>
    <automated>cd /home/zach/development/personal_clients/jam_coding/jaminator &amp;&amp; grep -c '^## wifi\|^## .*Private secrets\|^## .*Cache topology\|^## .*Threat model' docs/manifest-schema.md &amp;&amp; grep -c '"ssid"\|"authMode"\|"hidden"\|"autoConnect"\|"scope"\|"psk"' docs/manifest-schema.md &amp;&amp; grep -c 'application/vnd\.github\.raw\+json\|X-GitHub-Api-Version\|Contents: Read' docs/manifest-schema.md &amp;&amp; grep -c 'raw\.githubusercontent\.com.*ignores\|silently ignores\|Do NOT use ..raw' docs/manifest-schema.md &amp;&amp; grep -c 'WIFI-03\|PROJECT\.md' docs/manifest-schema.md &amp;&amp; grep -c 'operational opacity\|operational, not cryptographic\|operational security' docs/manifest-schema.md &amp;&amp; grep -c 'installer/secrets/README\.md' docs/manifest-schema.md &amp;&amp; grep -c '%ProgramData%..Jaminator..cache' docs/manifest-schema.md</automated>
  </verify>
  <acceptance_criteria>
    1. The four new section headings exist (case- and prefix-matched):
       - `grep -c '^## wifi$\|^## `wifi`$' docs/manifest-schema.md` returns at least `1` (Section 1).
       - `grep -ci 'private secrets channel' docs/manifest-schema.md` returns at least `1` (Section 2).
       - `grep -ci 'cache topology' docs/manifest-schema.md` returns at least `1` (Section 3).
       - `grep -ci 'threat model' docs/manifest-schema.md` returns at least `1` (Section 4).
    2. All six WifiProfileEntry JSON keys are documented (one per row in the field table):
       - `grep -c '`ssid`' docs/manifest-schema.md` ≥ 1
       - `grep -c '`authMode`' docs/manifest-schema.md` ≥ 1
       - `grep -c '`hidden`' docs/manifest-schema.md` ≥ 1
       - `grep -c '`autoConnect`' docs/manifest-schema.md` ≥ 1
       - `grep -c '`scope`' docs/manifest-schema.md` ≥ 1
       - `grep -c '`psk`' docs/manifest-schema.md` ≥ 1
    3. The three allowed `authMode` enum-like values are listed:
       - `grep -c 'WPA2PSK' docs/manifest-schema.md` ≥ 1
       - `grep -c 'WPA3PSK' docs/manifest-schema.md` ≥ 1
       - `grep -ci '"open"\|`open`' docs/manifest-schema.md` ≥ 1
    4. Both `scope` enum-like values are listed:
       - `grep -c 'all-users' docs/manifest-schema.md` ≥ 1
       - `grep -c 'current-user' docs/manifest-schema.md` ≥ 1
    5. The GitHub REST API endpoint form is documented (not raw.githubusercontent.com):
       - `grep -c 'api\.github\.com/repos' docs/manifest-schema.md` ≥ 1
       - `grep -c 'application/vnd\.github\.raw\+json' docs/manifest-schema.md` ≥ 1
       - `grep -c 'X-GitHub-Api-Version' docs/manifest-schema.md` ≥ 1
    6. The raw.githubusercontent.com anti-pattern is explicitly called out (so future maintainers don't "simplify" the fetch):
       - `grep -ci 'raw\.githubusercontent\.com' docs/manifest-schema.md` ≥ 1 (the URL itself is named in the anti-pattern callout)
       - `grep -ci 'silently ignores\|do not use.*raw\.githubusercontent\|ignored' docs/manifest-schema.md` ≥ 1
    7. The PAT scope is documented:
       - `grep -ci 'fine-grained' docs/manifest-schema.md` ≥ 1
       - `grep -c 'Contents: Read' docs/manifest-schema.md` ≥ 1
    8. The cache topology section names both files:
       - `grep -c 'manifest\.json' docs/manifest-schema.md` ≥ 1
       - `grep -c 'secrets\.json' docs/manifest-schema.md` ≥ 1
       - `grep -ci '%ProgramData%' docs/manifest-schema.md` ≥ 1
       - `grep -ci 'atomic' docs/manifest-schema.md` ≥ 1
    9. The threat-model section is candid (matches PROJECT.md WIFI-03 rationale):
       - `grep -ci 'operational opacity\|operational, not cryptographic\|operational security' docs/manifest-schema.md` ≥ 1
       - `grep -ci 'reverse-engineer\|recover the PAT\|extractable\|rot13' docs/manifest-schema.md` ≥ 1
       - `grep -ci 'termly\|rotation' docs/manifest-schema.md` ≥ 1
       - `grep -c 'WIFI-03' docs/manifest-schema.md` ≥ 1 (cross-reference to PROJECT.md)
       - `grep -ci 'netsh wlan show profile\|key=clear' docs/manifest-schema.md` ≥ 1 (the Windows-level PSK exposure reality)
    10. The operator runbook is cross-linked:
       - `grep -c 'installer/secrets/README\.md' docs/manifest-schema.md` ≥ 1
    11. PSK masking convention is documented:
       - `grep -c '\*\*\*' docs/manifest-schema.md` ≥ 1 (the literal `***` mask appears somewhere in the threat-model section)
    12. **Negative checks — content boundaries respected**:
       - `grep -ci 'netsh wlan add profile\|wifi profile runner\|WifiProfileRunner' docs/manifest-schema.md` returns 0 OR returns only forward-pointer mentions ("Phase 3"). The Phase 3 service is NOT documented here.
       - `grep -ci 'HARDEN-07\|CI automation' docs/manifest-schema.md` returns 0 (M3 is not promised as if shipped).
       - No real PAT value (any string matching `github_pat_[A-Za-z0-9_]{20,}`) appears anywhere — verify with: `! grep -E 'github_pat_[A-Za-z0-9_]{20,}' docs/manifest-schema.md`.
    13. Existing sections are byte-identical — verify by reading the first 182 lines of the file and confirming they match the current content. Specifically, the existing `# Manifest schema` H1, the opening paragraph, the top-level fields table at line 17, and the H2 sections for wallpaper / folders / programs / commands / cleanup are unchanged.
    14. `grep -c '^# Manifest schema$' docs/manifest-schema.md` returns exactly `1` (one H1 in the whole file — no duplicate top-level headings).
  </acceptance_criteria>
  <done>
    `docs/manifest-schema.md` has four new H2 sections appended in order: `wifi`, `Private secrets channel`, `Cache topology`, `Threat model — operational, not cryptographic`. All grep gates pass. The existing 182 lines are byte-identical. The cross-references to `installer/secrets/README.md` and `.planning/PROJECT.md` WIFI-03 are present. The raw.githubusercontent.com anti-pattern is explicitly called out so future maintainers don't "simplify" the fetch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer/operator reading the docs → their understanding of what Jaminator promises | The doc IS the trust boundary. A future maintainer who skims it must reach the correct conclusion about what's protected (PSKs off the public internet) and what isn't (PAT recoverable from the MSI). |
| Future code change author → the design decisions locked in WIFI-03 | The threat-model section is the load-bearing piece. If it's softened or removed, a future maintainer may "fix" the design back to encryption-at-rest (which was explicitly rejected) or unify the public + private fetches in a way that leaks the PAT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04 (doc surface) | Information Disclosure | Future code change that re-introduces raw.githubusercontent.com for the private fetch | mitigate | The "Fetch mechanism" H3 explicitly names `raw.githubusercontent.com` and explains why it does NOT work for private repos (Pitfall 1). Acceptance #6 requires the anti-pattern be named in the doc. A future maintainer Googling "raw.githubusercontent.com private repo" will hit our doc OR the GitHub community discussion (both cited in RESEARCH.md) and arrive at the same answer. |
| T-01 (doc surface) | Information Disclosure | A real PAT or PSK getting committed as an example value in the docs | mitigate | Acceptance #12 includes `! grep -E 'github_pat_[A-Za-z0-9_]{20,}' docs/manifest-schema.md` — any string that looks like a real fine-grained PAT fails the gate. Example values are placeholder strings only. |
| Threat-model softening | Tampering (intent) | A future maintainer rewriting the threat-model section to overstate Jaminator's guarantees (e.g., "PSKs are encrypted in transit and at rest" — technically true but misleading) | mitigate | The threat-model section quotes PROJECT.md WIFI-03 verbatim and includes the candid `netsh wlan show profile key=clear` reality. Acceptance #9 grep-gates the specific phrases. If a future PR softens the section, the gate fails and the checker flags it. |
| Spurious feature documentation | Logic Error | Documenting Phase 3 / Phase 4 features as if they're shipped in Phase 2 | mitigate | Acceptance #12 grep-rejects `WifiProfileRunner` and similar Phase 3 identifiers from appearing as anything other than a forward-pointer ("Phase 3"). |

This plan does not introduce or mitigate T-02 (PAT in binary), T-03 (cache half-write), or T-05 (open-network fallthrough) — those are implementation-side threats addressed by the code plans. The docs section names them honestly but does not mitigate them.
</threat_model>

<verification>
After Task 1:
1. All grep gates (#1-14) and negative gates (#12) pass.
2. Manual readability check: a non-technical Jam Coding staffer reading the new sections can answer:
   - "Where do PSKs live?" → "In a separate private GitHub repo, not in the public manifest."
   - "How do I add a new SSID?" → "Add an entry to the public manifest's `wifi.profiles[]` AND add the matching SSID→PSK line to the private `secrets.json`."
   - "What promises does this make?" → "PSKs don't appear on the public internet; the PAT in the MSI is recoverable by a determined attacker but bounded to read-only access on the private secrets repo."
3. A future Claude / human maintainer reading the "Private secrets channel" section will NOT try to "simplify" the fetch to use `raw.githubusercontent.com` — the anti-pattern callout is explicit.
</verification>

<success_criteria>
- `docs/manifest-schema.md` documents the new public `wifi.profiles[]` schema with all six WifiProfileEntry fields and their allowed values.
- The private `secrets.json` schema (flat SSID→PSK map) is documented with the canonical fetch URL form, headers, PAT scope, and the raw.githubusercontent.com anti-pattern.
- The cache topology is documented with both file paths, the atomic-pair write semantics, and the documented between-two-Moves rare-failure window.
- The threat-model section is candid, quotes PROJECT.md WIFI-03's rationale, and explicitly names the `netsh wlan show profile key=clear` Windows-level reality.
- Cross-references to `installer/secrets/README.md` (operator runbook) and `.planning/PROJECT.md` (Key Decisions) are present.
- Existing sections of the doc are byte-identical (only append).
- Maps to Phase 2 ROADMAP Success Criterion 1 (partial): "A private GitHub repo exists ... containing a secrets.json schema-documented in docs/manifest-schema.md" — the docs side is satisfied here; the repo-existence side is an operator pre-condition outside the plan's scope.
- Maps to WIFI-01: "documented in docs/manifest-schema.md alongside the existing manifest entries" — fully satisfied.
- Maps to WIFI-03: documentation of the private channel design including the threat-model honesty.
</success_criteria>

<output>
After completion, create `.planning/phases/02-private-secrets-channel-manifest-schema/02-04-SUMMARY.md`.

Commit per D-16 step 4: `docs(manifest-schema): document wifi.profiles[] and the private secrets channel`. Touch only `docs/manifest-schema.md`.
</output>
